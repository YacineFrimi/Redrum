# Redrum

Site du **Redrum**, bar alternatif à Tournai (BE).

Front dans `redrum-frontend/` — React 18 + Babel via CDN, pas de build, ouvrir `Redrum.html`.

Polices ITC Benguiat exclues du repo (licence commerciale, voir `.gitignore`).
